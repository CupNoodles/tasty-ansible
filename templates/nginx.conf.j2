server {
        listen 80 default_server;
        listen [::]:80 default_server;

        root {{ install_location }};

        index index.php;

        server_name _;

        include {{ install_location }}.nginx.conf;

}
